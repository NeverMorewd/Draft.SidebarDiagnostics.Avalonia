<Window
    x:Class="SidebarDiagnostics.App.Views.AboutWindow"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:SidebarDiagnostics.App.Models"
    Title="About Sidebar Diagnostics"
    Width="540"
    Height="570"
    x:DataType="models:AboutInfo"
    Background="{StaticResource BackgroundWindowBrush}"
    CanResize="False"
    ExtendClientAreaToDecorationsHint="True"
    ShowInTaskbar="False"
    WindowDecorations="BorderOnly"
    WindowStartupLocation="CenterScreen">
    <Grid
        Margin="28"
        RowDefinitions="Auto,Auto,Auto,*,Auto"
        RowSpacing="18">
        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="14">
            <Border
                Width="48"
                Height="48"
                Background="{StaticResource BackgroundAccentSubtleBrush}"
                CornerRadius="{StaticResource RadiusCard}">
                <Path
                    Width="24"
                    Height="21"
                    Data="M 1,18 L 7,11 L 12,14 L 22,3"
                    Stretch="Fill"
                    Stroke="{StaticResource TextAccentBrush}"
                    StrokeThickness="2" />
            </Border>
            <StackPanel
                Grid.Column="1"
                VerticalAlignment="Center"
                Spacing="2">
                <TextBlock
                    FontSize="{StaticResource FontSizeTitle}"
                    FontWeight="SemiBold"
                    Foreground="{StaticResource TextPrimaryBrush}"
                    Text="Sidebar Diagnostics" />
                <TextBlock
                    FontSize="{StaticResource FontSizeBody}"
                    FontWeight="SemiBold"
                    Foreground="{StaticResource TextAccentBrush}"
                    LetterSpacing="1.2"
                    Text="CROSS-PLATFORM SYSTEM MONITOR" />
            </StackPanel>
        </Grid>

        <TextBlock
            Grid.Row="1"
            FontSize="{StaticResource FontSizeDescription}"
            Foreground="{StaticResource TextMutedBrush}"
            Text="A modern Avalonia port of Sidebar Diagnostics for Windows, macOS, and Linux."
            TextWrapping="Wrap" />

        <Border
            Grid.Row="2"
            Padding="16"
            Background="{StaticResource BackgroundCardBrush}"
            BorderBrush="{StaticResource BorderPanelBrush}"
            BorderThickness="{StaticResource ThicknessBorder}"
            CornerRadius="{StaticResource RadiusCard}">
            <Grid
                ColumnDefinitions="140,*"
                RowDefinitions="Auto,Auto,Auto,Auto,Auto"
                RowSpacing="10">
                <TextBlock Foreground="{StaticResource TextSubtleBrush}" Text="Application version" />
                <TextBlock
                    Grid.Column="1"
                    Foreground="{StaticResource TextSecondaryBrush}"
                    Text="{Binding ApplicationVersion}" />
                <TextBlock
                    Grid.Row="1"
                    Foreground="{StaticResource TextSubtleBrush}"
                    Text="Avalonia" />
                <TextBlock
                    Grid.Row="1"
                    Grid.Column="1"
                    Foreground="{StaticResource TextSecondaryBrush}"
                    Text="{Binding AvaloniaVersion}" />
                <TextBlock
                    Grid.Row="2"
                    Foreground="{StaticResource TextSubtleBrush}"
                    Text=".NET runtime" />
                <TextBlock
                    Grid.Row="2"
                    Grid.Column="1"
                    Foreground="{StaticResource TextSecondaryBrush}"
                    Text="{Binding DotNetVersion}" />
                <TextBlock
                    Grid.Row="3"
                    Foreground="{StaticResource TextSubtleBrush}"
                    Text="Author" />
                <TextBlock
                    Grid.Row="3"
                    Grid.Column="1"
                    Foreground="{StaticResource TextSecondaryBrush}"
                    Text="{Binding Author}"
                    TextWrapping="Wrap" />
                <TextBlock
                    Grid.Row="4"
                    Foreground="{StaticResource TextSubtleBrush}"
                    Text="License" />
                <TextBlock
                    Grid.Row="4"
                    Grid.Column="1"
                    Foreground="{StaticResource TextSecondaryBrush}"
                    Text="{Binding License}" />
            </Grid>
        </Border>

        <StackPanel Grid.Row="3" Spacing="10">
            <TextBlock
                FontWeight="SemiBold"
                Foreground="{StaticResource TextSecondaryBrush}"
                Text="Open source" />
            <TextBlock
                Foreground="{StaticResource TextMutedBrush}"
                Text="Source code, releases, and project documentation are available on GitHub."
                TextWrapping="Wrap" />
            <Button
                HorizontalAlignment="Left"
                Click="OpenRepository"
                Content="Open repository" />
            <TextBlock
                Margin="0,6,0,0"
                FontWeight="SemiBold"
                Foreground="{StaticResource TextSecondaryBrush}"
                Text="Need help or found a problem?" />
            <TextBlock
                Foreground="{StaticResource TextMutedBrush}"
                Text="Please report bugs and feature requests through GitHub Issues."
                TextWrapping="Wrap" />
            <Button
                HorizontalAlignment="Left"
                Click="OpenIssues"
                Content="Report an issue" />
        </StackPanel>

        <StackPanel
            Grid.Row="4"
            HorizontalAlignment="Center"
            Spacing="8">
            <TextBlock
                HorizontalAlignment="Center"
                FontSize="{StaticResource FontSizeCaption}"
                Foreground="{StaticResource TextDimBrush}"
                Text="{Binding LicenseNotice}" />
            <Button
                MinWidth="88"
                HorizontalAlignment="Center"
                Click="CloseWindow"
                Content="Close" />
        </StackPanel>
    </Grid>
</Window>
